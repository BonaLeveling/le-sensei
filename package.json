{
  "name": "lesensei",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "start": "concurrently \"npm run dev --prefix lesensei\" \"npm run start --prefix lesenseiBack\"",
    "vercel-build": "cd lesensei && npm install && npm run build"
  },
  "dependencies": {
    "dompurify": "^3.2.7",
    "firebase": "^12.3.0",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2",
    "@google/generative-ai": "^0.11.0",
    "dotenv": "^16.4.5"
  }
}